# 设计系统

## 视觉方向

安静、紧凑的本地研究工具。设计服务于重复使用和证据扫描，不使用营销页构图或装饰性视觉元素。

## 色彩

沿用 `src/conflux/workbench/static/app.css` 中的现有变量：中性浅灰画布、白色工作面、深绿侧栏、青绿色主操作色，以及明确的成功、警告、错误和信息状态色。状态必须同时包含文字或图标。

## 字体

界面使用现有系统无衬线字体栈；代码、路径、提交哈希和原始输出使用等宽字体。页面标题保持 20px，工作面标题保持 16px，紧凑面板标题保持 12-13px。

## 布局

- 桌面端采用固定侧栏、顶部状态栏和受约束的主内容区。
- 操作密集页面使用表单与结果双栏；窄屏下按操作顺序折叠为单列。
- 页面区块保持无框或单层工作面，不嵌套装饰性卡片。
- 表格、列表和证据引用必须允许长路径换行或省略，并提供完整值。

## 组件

- 主要命令使用图标加文字按钮；工具命令使用 Lucide 图标按钮并提供标题。
- 二元模式使用分段控件或复选框，选项集合使用原生下拉菜单。
- 技术详情使用原生 `details/summary` 渐进披露。
- 状态标签保持稳定尺寸，使用文字区分成功、警告、错误和中性状态。
- 空状态说明用户下一步；错误状态同时提供恢复建议和可展开详情。

## 动效

仅为加载、状态变化和展开提供 150-250ms 反馈，使用现有缓动变量。遵循 `prefers-reduced-motion`，不添加装饰性入场动画。

## 响应式与无障碍

最低支持 320px 宽度和 200% 文本缩放。交互控件具备可见焦点，动态审计结果使用 `aria-live`，证据列表保持语义化结构且不依赖颜色表达含义。
