"""Adapters — bridge core protocols to concrete implementations.

This package contains adapters for:
- Plugin loading (plugin_loader.py)
- Model providers (future)
- Storage backends (future)
- CLI / HTTP (future)
"""
