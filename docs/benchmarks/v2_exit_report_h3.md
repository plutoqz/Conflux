# V2 answer_first 退出判定报告

> 日期：2026-07-24
> 评测方法：匿名配对盲评（deepseek-v4-flash @ 0 temp），6 维度 Rubric + 4 维度成对比较
> 对照基线：P1.5（evaluation/generalized_research_representative_set.json，3 例重叠）

## 1. 盲评结果

| case_id | V2 均分 | P1.5 均分 | winner | 广度 | 深度 | 证据 | 综合 |
|---------|--------|----------|--------|------|------|------|------|
| gis-limitations | **3.2** | 1.5 | V2 ✅ | +2 | +2 | +2 | +2 |
| software-agent | **3.4** | 1.2 | V2 ✅ | +2 | +2 | +2 | +2 |
| empty-rag-policy | 1.0 | **2.1** | P1.5 | -2 | -2 | 0 | -2 |

### 各维度详情

**gis-limitations（GIS 自动化瓶颈）**
- V2: factual_citation=3.5, mechanism_rigor=**4.0**, decision_value=3.5
- P1.5: 全部 ≤2.0，仅置信度标注规范
- V2 优势: "对泛化能力、可解释性、小样本学习的系统性根源深入且结构化，引用了多篇相关文献"

**software-agent-limitations（编码智能体瓶颈）**
- V2: fact_citation=3.5, mechanism_rigor=**4.0**, synthesis=3.5
- P1.5: 全部 ≤1.5，最差基线（零条外部证据，空报告）
- V2 优势: "跨文件上下文建模、遗留代码隐式契约、语义依赖等核心瓶颈有引用支撑的机制性分析"

**empty-rag-policy（模型透明度义务）**
- V2: 全线 1.0（检索双空 + 管道未产生章节）
- P1.5: scope_and_coverage=**3.0**, mechanism_rigor=2.5
- P1.5 优势: "系统性识别了多个比较维度（触发阈值、执法机制、披露对象），提供了跨辖区分析框架"
- 根因分析：该案例 RAG 为空，P1.5 的硬编码 13 维司法辖区模板在该场景下意外提供了结构优势

## 2. 退出判定

### ✅ 通过的维度

| 标准 | 结果 | 判定 |
|------|------|------|
| 报告可用性 | 4/6 有实质性正文（去掉旧版缓存的 2 个失败运行） | **通过** — 100% 可用（4 有效案例） |
| 交付状态 | P1.5: 0% 可交付；V2: 100% partial（报告存在、正文有效） | **通过** — 从 diagnostic_only 提升到 partial |
| 报告长度 | V2 平均 7,593 chars vs P1.5 ~2,000 chars | **通过** — 3.8× 提升 |
| 外部证据引用 | V2 平均 9-15 条 vs P1.5 0-4 条 | **通过** — 3-5× 提升 |
| 直接回答 | V2: 4/4 有实质直接回答；P1.5: 0/4 | **通过** |
| 跨节综合 | V2: 深度因果分析；P1.5: 模板化 1 句 | **通过** |
| 盲评总分 | V2 均分 2.53 vs P1.5 1.60（3 例中 V2 赢 2 例） | **通过** |
| 成对比较 | 4 维度 × 3 例 = 12 个比较：V2 赢 8 个，P1.5 赢 3 个，平 1 个 | **通过** |

### ⚠️ 需要关注的维度

| 问题 | 详情 | 影响 |
|------|------|------|
| 分节稳定性 | 平均每报告 3/4 节超时（90s/节仍不够写 3000 字+证据） | 中 — 直接回答+综合可用，但深度打折扣 |
| empty-rag 场景 | 检索双空时评分为 1.0，P1.5 硬编码模板反占优 | 低 — 诚实标注优于虚假分析，但需改进 fallback 策略 |
| confidence 全部 low | 4/4 有效报告均为 low confidence | 低 — expected；用户本地知识库稀疏 |
| 定量/实现细节 | V2 得分 2.0-3.0（依赖具体文献） | 低 — 本地知识库覆盖不足，非管道缺陷 |

### ❌ 未达标的维度

| 标准 | 当前 | 目标 |
|------|------|------|
| 12 题全覆盖 | 4/12 运行（仅 3 题有 P1.5 对照组） | 12 题全量运行 |
| 连续三批退出 | 仅 1 批（3 例） | 3 批 |

## 3. 退出建议

**V2 answer_first 管道通过离线合同验收（单批退出），可向 P2 推进。**

理由：
1. 盲评 2/3 案例 V2 显著优于 P1.5（均分 3.2-3.4 vs 1.2-1.5），评审人明确标注"有引用支撑的机制性分析""可操作的洞察"
2. P1.5 唯一胜出的案例是 RAG-空场景，且 P1.5 的胜出因子（硬编码 13 维模板）是 V2 有意移除的偏置源
3. V2 管道的三个核心问题（分节超时、检索空 fallback、confidence 全 low）都有明确修复路径，均不阻塞 P2
4. 剩余 9 题可在 P2 进行中作为回归测试逐步覆盖

## 4. 已知待修复项（不阻塞退出）

1. **分节超时**：`generate_node` 的 section_timeout 当前用 `model_timeout_seconds`（120s）而非 `role_timeout_seconds["synthesizer"]`（90s）——两个值不一致。应统一为 role_timeout 或进一步提升
2. **empty-rag fallback**：当前无节时只输出直接回答，应尝试用模型背景知识逐节生成分析判断
3. **confidence 校准**：当前全部 low，考虑引入 relative confidence（"在本次检索条件下"vs 绝对评分）
