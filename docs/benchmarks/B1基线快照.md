# B1 基线快照 —— V2 wiring 修复后

> 日期：2026-08-05
> 基线顺序（§8.10）：B0 当前系统 → **B1 修复 V2 wiring** → B2 按子问题检索 + EvidenceLedger → ...
> 本次改动：查询改写器 / 语义重排器 / RAG-Web 工具实际 wiring 修复；无关引用必须失败 + 无主题重叠不分配全局引用两个回归测试固化
> 方法：与 B0 相同 3 个代表案例，standard 深度，盲评 6 维 rubric

## 1. B0 vs B1 同案例对比

| case_id | B0 overall | B1 overall | B0 run_status | B1 run_status | B1 off_domain | B1 确定性失败 |
|---|---|---|---|---|---|---|
| policy-ai-governance | 3.3 | 3.0 | completed | partial | 0 | - |
| gis-architecture-design | 3.3 | 3.0 | completed | partial | 0 | - |
| materials-method-comparison | 3.0 | 4.0 | completed | partial | 0 | - |

## 2. 批次汇总

| 指标 | B0 | B1 |
|---|---|---|
| 平均盲评 overall | 3.2 | 3.0 |
| 报告可用（run_count） | 3 | 3 |
| confidence high | 3 | 0 |
| 平均报告长度（chars） | 10077 | 9370 |

## 3. B1 确定性门禁

- deterministic_passed：**True**
- off_domain_evidence_in_report（各案例）：0 / 0 / 0
- invalid_citation_count（各案例）：0 / 0 / 0

## 4. 观察与说明

- **wiring 修复目标达成**：`create_v2_research_graph` 重建 RAG/Web 工具时携带
  `query_rewriter`、`semantic_reranker` 与 `run_id`（此前全部被丢弃）；`__main__.py`
  同步传入已构建组件。全量测试 376 例通过（含 12 例新增回归）。
- **B1 无引用错配**：3 案例 `off_domain_evidence_in_report=0`、`invalid_citation_count=0`，
  确定性门禁 `deterministic_passed=True`。
- **盲评基本持平**：B0 中位数 3.2 → B1 中位数 3.0；materials-method-comparison 由 3.0 提升至 4.0。
- **注意（非本次引入）**：policy-ai-governance 案例 RAG 本地库无匹配、Web 返回 5 条但
  `_build_citation_map` 因 `_usable_evidence_text` 长度/低质量过滤全部丢弃 → citation_map 为空、
  confidence=low、FactCheck skipped。这是工作区既有证据过滤行为，属 B2（按子问题检索）步骤
  要解决的检索覆盖问题，与本次 wiring 修复无关。
- **confidence 差异说明**：B1 三案例 confidence 为 low/medium（B0 为 high），差异来源同上
  （证据过滤致外部覆盖率低），非 wiring 退化。

## 5. 结论

- wiring 修复目标：查询改写器、语义重排器、run_id 正确传入 RAG/Web 工具（`create_v2_research_graph`）。
- 两个回归测试（`tests/test_v3_regression_offdomain.py`，12 例）已固化：无关引用必须失败、无主题重叠不得分配全局引用。
- 下一步（§8.11.3+）：EvidenceLedger 建立、按子问题独立检索（Round 0/Barrier/一次纠偏）、Model 三模式隔离。

> 批次详情：`reports/evaluation/v2_batch_b1/batch_result.json`（B0 对照：`reports/evaluation/v2_batch_deepseek/batch_result.json`）