# Conflux 项目总体进展报告

> 生成日期：2026-08-01
> 本报告汇总全部阶段（A/R/B/C、V2 H-K、P2/M3）的实际状态，基于已提交成果与实验记录。

---

## 1. 项目定位与管道现状

- **目标**：多源（RAG + Web + Model）研究型 Agent，产出可审计、可追溯的研究报告。
- **当前管道**：**V2 `answer_first`**（decompose → retrieve → generate → synthesize → audit → finalize）为**唯一**管道（P1/P1.5 已删除）。
- **生产模型**（config.yaml）：`flash`/`balanced`/`verifier` = **deepseek-v4-flash-guan**（dmxapi，实测可用；带推理模式，`reasoning_content` 先行）。
- **检索**：hybrid（BM25 0.3 + dense 0.7，RRF 融合），top_k=10 / final_k=5（管道）；评测建议 top_k=60 候选池。

---

## 2. 里程碑时间线（近期）

| 提交 | 内容 | 状态 |
|---|---|---|
| `f632477` | V2 H-K 框架闭合 + 阶段 R 评测框架就绪（同步） | ✅ |
| `a59e953` | P2 paper radar 论文雷达子系统 + Workbench 集成 | ✅ |
| `75d3b99` | **V2 I/J/K 收尾**：参数调优、硬编码退出主路径、P1/P1.5 废弃代码清理（-18.6k 行） | ✅ |
| `65116bf` | **R1 检索消融实验**：4 embedding × 3 chunk + 4 rerank + 跨语言验证 | ✅ |

---

## 3. 阶段状态总表

### 3.1 研究查询重设计（V2 路线 A–K）

| 阶段 | 内容 | 状态 |
|---|---|---|
| A–G | V2 管道实现与离线合同 | ✅ |
| **H** | V2 与 P1.5 基线 A/B 盲评 | ✅ 单批通过（V2 退出报告 h3） |
| **I** | 参数调优 | ✅ 盲评驱动：GLOBAL_PROMPT 增强行动建议/定量取舍 |
| **J** | 硬编码退出主路径 | ✅ BILINGUAL_TERM_MAP 降级为 fallback（LLM 改写优先） |
| **K** | 废弃代码清理 | ✅ P1/P1.5 全套删除 |

### 3.2 Agent书籍优化计划修正路线（A / R / B / C / D / E / F-I）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **A** | 闭合 V2 H-K（真实 API 盲评） | 🔶 **约 60%**：12 题覆盖 6/12；**连续三批退出 ✅**（3 题 PASS，均分 3.2，deepseek-v4-flash-guan）；A4 硬编码清理已由 I/J/K 覆盖 |
| **R** | 组件级评测数据收集 | 🔶 **R1 ✅ / R2 ✅ / R3 脚本就绪；R4 终止**（见 §4） |
| **B** | 评估/安全基线补强（1-2 周） | ⬜ 未启动 |
| **C** | 裁剪版 Agent 状态栏（1 周） | ⬜ 未启动 |
| **D** | Skills 验证（条件触发） | ⏸️ 等待触发 |
| **E** | 项目级偏好 + 策略复用 | ⬜ 未启动（与 M3 联合） |
| **F–I** | MCP / Agentic RAG / 多 Agent（条件触发） | ⏸️ 等待触发 |

### 3.3 执行计划 v1（P2 / M3）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P2** | Paper Radar 论文雷达 | 🔶 核心协议/引擎/测试已实现（`a59e953`）；**真实 API 联调、标注集、退出标准待完成** |
| **M3** | 持久化（SQLite/Postgres） | ⬜ 待启动（排在 P2 后） |

---

## 4. 阶段 R — 组件级评测明细

| 项 | 脚本 | 数据集 | 运行状态 | 结论/待办 |
|---|---|---|---|---|
| **R1** RAG 检索消融 | `scripts/eval_rag_ablation.py`（重写） | `data/rag_eval/` 三语各 10 题 | ✅ **完成**（12+4+5 组） | 见 §5 |
| **R2** Web 搜索质量 | `scripts/eval_web_search.py` | `data/web_eval_queries.yaml`（18 题） | ✅ **完成** | 标注完成（均分 2.06，见 `reports/eval/web/`） |
| **R3** P1 消融 | `scripts/eval_ablation.py` | 3×4=12 组矩阵 | ✅ 脚本已修复可跑 | 全量运行确认 |
| **R4** 端到端门禁 | `scripts/eval_gates.py` | V2 批 4 runs | ✅ **已终止**（2026-08-02 决定不再执行） | 不再列入待完成/未完成项 |

---

## 5. 关键实验结论

### 5.1 V2 三批盲评（2026-08-01，deepseek-v4-flash-guan）

| 用例 | overall | 判定 |
|---|---|---|
| policy-ai-governance | 3.3 | PASS |
| gis-architecture-design | 3.3 | PASS |
| materials-method-comparison | 3.0 | PASS |

- 连续三批成功 → 提前停止；3 份报告全部 `conf=high`、8.6k–11k 字符
- 结果存放：`reports/evaluation/v2_batch_deepseek/batch_result.json`

### 5.2 R1 检索消融（ranx 指标，NDCG@10）

**底座选择（zh-zh）**：

| Embedding | 最佳 ndcg@10 | 备注 |
|---|---|---|
| `text-embedding-v4` | **1.000**（全档） | 基准标杆胜出 |
| `qwen3-embedding-8b` | 1.000（全档） | 与 v4 并列但 4096 维开销大 |
| `jina-embeddings-v4` | 0.963 | 接近 |
| `bge-m3` | 0.963 | 1024/256 档仅 0.819 |

**跨语言（zh-en，中文 query → 英文文档）**：

| Embedding | ndcg@10 | 结论 |
|---|---|---|
| `text-embedding-v4` | **0.819** | 跨语言最强 |
| `jina-embeddings-v4` | 0.815 | 接近 |
| `qwen3-embedding-8b` | 0.806 | **8B 未兑现优势** |
| `bge-m3` | 0.682 | 混合检索垫底 |

**rerank（3 数据集 × 4 方法）**：12 组合 **10 降、1 平、1 微升**（en-en jina +0.013）→ **rerank 不推荐默认**。

**最终配置建议**：`text-embedding-v4` + chunk `512/128` + hybrid top_k=60，**不启用 rerank**。

---

## 6. 当前生产配置 vs 建议

| 项 | 当前（config.yaml） | R1 建议 |
|---|---|---|
| embedding | `text-embedding-3-small` | **`text-embedding-v4`（待切换）** |
| chunk | 1024/256 | 512/128（zh-en 场景） |
| rerank | SemanticReranker（LLM） | 关闭或 jina-reranker-m0（可选） |
| chat | deepseek-v4-flash-guan | 保持 |

---

## 7. 资产清单

- **评测数据集**：`data/rag_eval/{zh_zh,zh_en,en_en}.yaml`（R1）、`data/web_eval_queries.yaml`（R2）、`data/p1_retrieval_eval_{zh,en}.yaml`（旧标签）
- **评测脚本**：`scripts/eval_rag_ablation.py`、`eval_web_search.py`、`eval_ablation.py`、`eval_gates.py`、`run_v2_blind_batch.py`
- **报告**：`reports/eval/rag_ablation/`（R1 全套）、`reports/evaluation/v2_batch_deepseek/`（盲评）、`docs/benchmarks/V2退出报告h3.md`
- **测试**：287 个全部通过（`python -m pytest`）

---

## 8. 下一步建议（按优先级）

1. ~~R2 人工标注~~ ✅ 已完成（2026-08-02，均分 2.06/3，见 `reports/eval/web/web_search_annotations.md`）
2. **生产配置切换**：`text-embedding-v4` + 512/128（R1 结论落地）
3. **B 阶段**（评估/安全补强 1-2 周）：P1 Rubric 适配 V2、V2 回归测试、消毒扩展 RAG 路径
4. **C 阶段**（状态栏 1 周）：两句式状态栏 + 预算告警
5. **P2 真实 API 联调** + M3 持久化（更远期）

---

## 9. 2026-08-03 状态勘误

本节追加记录 2026-08-01 快照之后的变化。上文表格保留当时状态；判断当前进度时以本节和 `docs/plans/执行计划v1.md` 为准。

### 9.1 已发生的状态变化

- **生产检索配置已切换**：`text-embedding-v4`、chunk 512/128、BM25 0.3 + dense 0.7 已进入 `config.yaml`，上文第 6 节“待切换”已经过期。
- **B 阶段已完成**：V2 Rubric 回归、RAG 路径消毒、工具参数保真和安全基线已于提交 `761b6da` 落地。
- **C 阶段已完成**：两句式 Agent 状态栏、时间/Token 剩余量和 30% 预算告警已于提交 `0524404` 落地。
- **P2 已完成首轮真实联调**：真实来源冒烟得到 29 个候选、去重后 17 个；LLM 深度分析调用 3 次，其中 1 次 fallback。该结果只证明链路可运行，不代表筛选质量达标。
- **M3 及以后仍未启动**：尚无 SQLite Run Store、持久 Job、初始化迁移或 Evidence Ledger。

### 9.2 评测和测试勘误

- 当前测试收集数为 **345**，不是上文记录的 287。
- 分模块执行时 344 个测试通过；`tests/test_p2_paper_radar.py::TestRadarPipeline::test_radar_writes_output` 会访问真实 arXiv PDF，导致全量“离线”测试长时间挂起。
- 最新三份 V2 真实报告的内部正文评分为 3.0–3.3 / 5；抽查报告尚未通过现有 `conflux.acceptance`，缺少旧合同要求的来源状态、Evidence sidecar、运行摘要和质量门信息。
- R3 现有 `scripts/eval_ablation.py` 使用 FakeModel 和旧式配置，只能保留为历史合同测试；当前 V2 的真实组件消融需要重新设计。
- R4 已终止，不再列入未完成项。

### 9.3 当前产品问题

1. Workbench 项目监控可能读取过期快照，当前页面状态不一定等于实时 Git 状态。
2. 测试报告、Trace 和 Summary 可能进入成果统计，造成研究成果数量失真。
3. V2 正文质量评审与报告产物验收使用不同合同，需要统一。
4. Paper Radar 的 LLM 失败 fallback 与执行计划要求的 `unreviewed` 语义不一致。

### 9.4 后续优先级

1. 同步 README、执行计划、进展报告和文档索引的当前状态。
2. 按研究者完整工作周期实际使用 Conflux，修复阻断、错误状态和产物可信度问题。
3. 在体验稳定后补齐端到端样例、各阶段耗时、Token、成本和质量评分数据。

## 10. 2026-08-03 阶段 2 可用性验证

本轮按研究者使用链路执行离线检查并修复阻断，不新增产品范围。

### 10.1 结果

- Paper Radar 不再因夹具中的 arXiv ID 隐式下载 PDF，全量离线测试恢复稳定。
- V2 新报告同时生成 Markdown、HTML、Evidence、Sources 和 Audit，确定性完整样例通过 CLI acceptance；查询 CLI 的产物路径打印和摘要回填可用。
- Workbench 项目监控实时反映当前本地 HEAD 和未提交状态，远程提交明确标记为缓存且需手动检查；成果统计与列表统一为 11 份正式 Markdown 报告，不再计入测试、评测、Trace、JSON、HTML 副本和草稿。
- Workbench 项目接口从约 10.16s 降至后台服务约 1.2-1.3s。桌面与 390px、320px 移动端的总览、项目监控、成果列表及报告预览未发现横向溢出或不可访问控件。
- 全量测试结果为 352/352 通过；V2 acceptance CLI 结果为通过，所有 11 项 V2 合同检查为真。

### 10.2 仍需真实使用确认

- 本轮未发起新的真实付费模型查询，不能更新真实内容质量、延迟、Token、成本和评分结论。
- 历史 V2 报告不会自动补齐新 sidecar；它们的 3.0-3.3 / 5 评分仍是历史结果，不代表当前生成链路已达到内容质量退出线。
- P2 标注集、跨项目隔离、退出标准及 LLM fallback 的 `unreviewed` 语义仍待后续确认。
- M3 未启动，进程重启后的运行恢复仍不可用。

## 11. 2026-08-04 阶段 2 阻断修复与运行态复核

本节追加记录用户真实使用发现的知识入库、报告评分、计划分析和研究雷达问题。

### 11.1 已修复

- 知识入库在写入前校验 Embedding 模型与 collection 维度；1024/1536 不匹配时返回结构化错误，不再直接暴露 Chroma 异常，也不会修改旧索引。
- Workbench 支持从全部知识 Markdown 完整重建到新 collection，成功后才切换；旧 collection 默认保留，且只能由用户主动删除，当前 collection 禁止删除。
- 模型页保留研究查询快速、标准、深度三档，并为研究画像优化、论文语义评审、项目计划分析、项目纲领生成、研究运行雷达、计划条目翻译提供六套独立配置和连接测试。
- V2 正式报告不再追加旧版质量评分，Workbench 也不再用旧 staged Markdown 覆盖 V2 报告。查询 `4916ced2f4be` 未在本轮重新运行，因此只确认评分串线已修复，不重新判定其内容质量。
- 项目计划分析启用 JSON mode 和更高输出上限；Conflux 项目真实分析成功，耗时 43.4 秒，生成 9 个可审查条目，产物为 `reports/workbench/projects/conflux/plan_analysis.json`。
- Paper Radar 使用现有确定性论文评分完成粗排，只有分数达到 0.62 的候选进入 `shortlisted` 和深度分析；不再在 `shortlisted=0` 时默认精读前 5 篇。所有来源失败、无候选、无精读候选和可审查结果分别返回明确状态。

### 11.2 运行态结果

- 全量测试为 360/360 通过，耗时 63.61 秒；模型配置、索引生命周期、报告输出、计划分析和雷达候选选择均有聚焦回归测试。
- `127.0.0.1:8766` 已用最新代码重启。页面显示六个功能模型；知识库显示当前 `conflux_docs` 为 97 条、1024 维，当前 collection 无删除按钮，完整重建入口可见。
- `paper_review` 真实连接探测成功：模型 `qwen3.7-plus`，耗时 6.514 秒，实际 usage 为 326 tokens，其中 298 reasoning tokens。该结果说明供应商实际 token 消耗可能高于请求的简短输出上限，阶段 3 必须记录响应 usage，而不能只按请求参数估算。

### 11.3 未覆盖与剩余风险

- 本轮未对真实知识库执行完整重建，避免在用户确认前生成和切换新 collection；重建、保留旧索引和主动删除使用临时 Chroma collection 完成测试。
- Paper Radar 粗排和状态合同已修复，但尚未用真实来源重新运行；候选阈值、排序质量、标题展示和项目管理价值仍需用户真实使用与 P2 标注集验证。
- `4916ced2f4be` 未重新生成，不能据此声明研究报告已经通过新的质量验收。
- M3 持久运行仍未启动，服务重启后的任务恢复不属于当前能力。

### 11.4 模型切换与知识库真实重建复核

- 2026-08-04 按用户更新后的配置复核：研究查询快速、标准、深度三档与六个功能模型均解析为 `deepseek-v4-flash-guan`，Embedding 模型为 `text-embedding-v4`，相关调用凭证可用。
- 已真实完整重建并切换到 `conflux_docs__text-embedding-v4__20260804_142039`：190 条、1024 维，collection metadata 中记录模型为 `text-embedding-v4`。
- 旧 `conflux_docs` 保留为非当前 collection：97 条、1024 维，未删除。
- 使用查询 `knowledge graph GIS agent research workflow` 对新 collection 实际检索成功，返回 3 条结果，最高相似度分数为 0.5794，覆盖全文分块和摘要分块。
- `standard` 模型真实连接耗时 1.197 秒，供应商响应模型名为 `deepseek-v4-flash`，usage 为 101 tokens，其中 16 个 completion tokens 全部记为 reasoning tokens；由于 `max_tokens=16`，正文输出被截断。该结果只证明连接可用，短输出任务仍需在阶段 3 验证合理的输出上限与真实 token 消耗。

## 12. 2026-08-04 阶段 2 第二轮真实使用修复

本轮针对用户再次使用后确认的两个问题收敛状态语义和项目监控数据来源。

### 12.1 研究查询

- 每个扩展章节现在记录完成状态、耗时、错误和模型可用的 usage；任一章节失败时，运行状态为 `partial`、交付状态为 `limited`，Workbench 明确显示未完成扩展问题数量。
- FactCheck 只表示已生成声明的引用核验结果，不再被解释为整份报告完整通过。
- 历史运行 `024d784b27cb` 实际只完成 3/6 个扩展问题；历史产物不会自动改写，本轮也未重新发起真实付费查询。

### 12.2 项目监控

- 有缓存时仍实时刷新本地 Git、计划文档、报告、审计和计划上下文；远程名称、URL、分支和远程 HEAD 继续保留为缓存信息。
- Conflux 计划来源优先读取 `docs/plans/执行计划v1.md`、`docs/架构设计.md`、`docs/plans/done/项目总体进展报告.md` 和 `PRODUCT.md`，不再由 README 抢占计划分析上下文。
- 报告统计只计顶层和 Workbench 查询目录中的正式 Markdown，排除 HTML、JSON、评测、测试、中间稿、核验稿和诊断稿。总览、侧栏成果和项目证据均显示 13 份报告。

### 12.3 验证结果

- 聚焦测试 98/98 通过；全量测试 361/361 通过，耗时 53.87 秒。
- `node --check src/conflux/workbench/static/app.js` 与 `git diff --check` 通过；后者仅有既存 CRLF/LF 提示。
- Workbench 已在 `http://127.0.0.1:8765/` 使用最新代码运行，8766 无监听；浏览器复核总览、项目监控证据页和控制台未发现新的错误。

### 12.4 仍需真实使用确认

- 新的 `partial/limited` 状态合同已通过确定性测试，但尚未用真实模型重新触发一次扩展章节超时。
- 项目监控当前可展示人工计划、实时本地证据和 AI 分析入口；其管理价值仍需用户用真实项目节奏验证，尤其是计划条目是否足够清楚、可执行。

## 13. 2026-08-04 阶段 2 第三轮真实阻断修复

本轮根据真实运行 `d32da1636344` 定位并修复研究查询预算耗尽与项目计划分析超大上下文问题。

### 13.1 研究查询

- 真实运行计划生成 6 个扩展章节，实际完成 4 个；前四章合计使用 125,351 tokens，后两章因 `140000` 总预算和并行预留在模型调用前被拒绝。
- Deep 档总 Token 上限从 140,000 放宽到 320,000，保留总预算保护，不改模型、章节数、评估指标或重试策略。
- `V2State` 补充 `_delivery_status` 和 `_delivery_assessment`，避免 `partial` 在完整图中错误回退为 `diagnostic_only`。

### 13.2 项目计划分析

- 计划分析不再读取整个 `docs/` 目录；存在 `plan.source_documents` 时，只使用明确登记的来源和正式项目纲领。
- Conflux 实际输入由 18 份文档、217,131 字符缩减为 4 份文档、98,617 字符，粗略 Token 规模由约 108k 降至约 49k。
- 单次输出上限由 7,200 放宽到 12,000 tokens，模型调用超时由 150 秒放宽到 240 秒。
- 分析与人工确认应用使用相同的来源哈希口径，避免分析成功后因额外文档被错误判定为来源变化。
- 后续计划分析结果会保存模型耗时、usage、是否经过结构修复和输入文档数量；未进入 Prompt 的历史文档截断警告不再显示。

### 13.3 真实验证

- 使用 `deepseek-v4-flash-guan` 真实分析 Conflux 项目计划成功，耗时 138.19 秒；供应商返回模型名为 `deepseek-v4-flash`，生成 6 个里程碑和 4 个后续行动。
- 所有条目的文档来源均来自 `docs/plans/执行计划v1.md`、`docs/架构设计.md`、`docs/plans/done/项目总体进展报告.md` 和 `PRODUCT.md`。
- 聚焦测试 99/99、全量测试 362/362 通过；Workbench 已在 `127.0.0.1:8765` 使用最新代码运行。

### 13.4 未覆盖与风险

- 本轮未重新运行 Deep 真实研究查询，320,000 上限能解除已确认的并行预算拒绝，但完整 6 章节的实际完成率和总成本仍需下一次真实使用确认。
- 320,000 是放宽后的硬上限，不是目标消耗；当前每章重复约 24k 输入的问题仍会造成较高成本，后续应基于阶段 3 数据判断是否缩减章节上下文。
- 本次真实计划分析发生在 usage 持久化代码加入之前，因此只记录到总耗时，Token usage 需由下一次运行采集。

## 14. 2026-08-04 阶段 2 第四轮 Deep 真实使用复核

本轮检查用户运行 `48052d7e48a4` 的完整产物，并修复运行成功后仍存在的引用质量、FactCheck 语义和输入成本问题。

### 14.1 真实运行结论

- Deep 查询完成 6/6 个章节，无失败或截断，总耗时 238.48 秒；六章合计使用 248,623 tokens，其中输入 199,509、输出 49,114，占 320,000 总预算约 77.7%。
- 该运行证明 320,000 上限已解除章节调用前的预算阻断，但每章约 33.25k 输入，实际重复发送了完整证据上下文。
- 原 FactCheck 的 44/44 通过不代表声明级核验：实现把章节全部引用复制给章节内每条声明，导致 `citation_refs_used=514`，结论语义虚高。
- 15 个引用条目中存在摄取元数据、网页导航残片、孤立短句和同一 arXiv 论文的重复 HTML/PDF 或多摘录编号；报告同时出现重复“可信度说明”，并由模型把 15 个引用改写为 16 条。

### 14.2 已修复

- 引用构建过滤摄取元数据、网页导航残片和过短证据；同一 arXiv 论文、URL、内容哈希或文档标识合并为一个来源编号，最多合并两段不同证据。
- 章节只接收与子问题排序后的最多 10 个去重引用条目，不再重复发送完整 RAG/Web 原始载荷；原始来源仍保存在 Sources 产物中供审计。
- 结构化摘要要求每条 `claim:` 自身保留直接支持它的引用编号；FactCheck 只认可声明文本内且存在于全局引用表的编号，不再继承章节级引用。
- 可信度章节的章节数、完成数、失败数、截断数、引用数和证据缺口数改由确定性指标生成；存在证据缺口时不再给出 `high`，最终组装会移除模型遗留的重复标题。

### 14.3 验证与边界

- 使用该次运行的原始 Sources 离线回放：125,384 字符的来源载荷被整理为 5 个去重来源编号，示例章节 Prompt 为 3,686 字符，且不包含 `CONFLUX_SOURCE_RESULT_JSON` 或原始大载荷。
- 聚焦测试 23/23、全量测试 366/366 通过；Workbench 已以进程 73976 在 `http://127.0.0.1:8765/` 重启，`/api/status` 返回 HTTP 200。
- 本轮未再次发起付费 Deep 查询，因此只能确认数据路径、统计语义和输入裁剪生效，不能提前声明新报告的内容覆盖、声明引用率或最终评分已经达标。
